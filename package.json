{
  "name": "invoice-brecx",
  "version": "0.1.0",
  "private": true,
  "description": "Invoice Brecx: create, send and track invoices for Brecx clients.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "start:api": "pnpm --filter @inv/api run start"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  }
}
